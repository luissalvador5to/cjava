eureka:
  client:
    instance:
      preferIpAddress: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
server:
  port: ${random.int(3370,3372)}
  servlet:
    context-path: /microservicio
spring:
  application:
    name: MICROCURSO
  data:
    mongodb:
      database: cursoDB
      host: localhost
      port: 27017
